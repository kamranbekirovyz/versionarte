name: versionarte
description: A Flutter package for determining update availability besides with some helpful features and pre-canned UI-kit.
version: 0.3.0
repository: https://github.com/kamranbekirovyz/versionarte
home: https://github.com/kamranbekirovyz/versionarte

environment:
  sdk: '>=2.12.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_remote_config: ^3.0.9
  http: ^0.13.5
  package_info_plus: ^3.0.2
  url_launcher: ^6.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0